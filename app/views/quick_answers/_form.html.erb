<%= form_for(@quick_answer) do |f| %>
  <% if @quick_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quick_answer.errors.count, "error") %> prohibited this quick_answer from being saved:</h2>

      <ul>
      <% @quick_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :answer %><br />
    <%= f.text_area :answer %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :quick_question_id %><br />
    <%= f.number_field :quick_question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
