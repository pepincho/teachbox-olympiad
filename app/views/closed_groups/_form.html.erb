<%= form_for(@closed_group) do |f| %>
  <% if @closed_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@closed_group.errors.count, "error") %> prohibited this closed_group from being saved:</h2>

      <ul>
      <% @closed_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field", style="display: none;">
    <%= f.label :admin_id %><br />
    <%= f.number_field :admin_id, :value => current_user.id %>
  </div>
  <div class="field">
    <%= f.label :decription %><br />
    <%= f.text_area :decription %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
