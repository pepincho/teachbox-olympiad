<%= form_for(@comments_open_group_post) do |f| %>
  <% if @comments_open_group_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comments_open_group_post.errors.count, "error") %> prohibited this comments_open_group_post from being saved:</h2>

      <ul>
      <% @comments_open_group_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :open_group_post_id %><br />
    <%= f.number_field :open_group_post_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
