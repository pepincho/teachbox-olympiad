<h1>Listing open_groups</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @open_groups.each do |open_group| %>
  <tr>
    <td><%= open_group.description %></td>
    <td><%= open_group.name %></td>
    <td><%= link_to 'Show', open_group %></td>
    <td><%= link_to 'Edit', edit_open_group_path(open_group) %></td>
    <td><%= link_to 'Destroy', open_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Open group', new_open_group_path %>
