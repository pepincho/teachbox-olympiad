<%= form_for(@open_group) do |f| %>
  <% if @open_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@open_group.errors.count, "error") %> prohibited this open_group from being saved:</h2>

      <ul>
      <% @open_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
